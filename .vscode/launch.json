{
  "configurations": [
    {
      "name": "Launch via bun",
      "type": "node",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "runtimeExecutable": "bun",
      "runtimeArgs": ["start", "git-commit"]
    }
  ]
}
