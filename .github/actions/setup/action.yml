name: Setup Bun
description: Install Bun with caching

inputs:
  install-args:
    description: Additional args for bun install
    required: false
    default: ""

runs:
  using: composite
  steps:
    - uses: oven-sh/setup-bun@v2

    - run: bun install ${{ inputs.install-args }}
      shell: bash
