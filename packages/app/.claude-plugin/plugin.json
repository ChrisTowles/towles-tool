{
  "name": "towles-tool-app",
  "description": "Bridges Claude Code to the towles-tool desktop app: points at its loopback-HTTP MCP server for board tasks, showing an HTML page you wrote in the app's Preview pane, and calendar reads/writes, plus a hook that nudges the app to refresh PR/issue data immediately after a `gh pr`/`gh issue` mutation. Requires the app to be running.",
  "version": "0.1.18",
  "author": {
    "name": "Chris Towles"
  }
}
