[package]
name = "tt-mcp"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
tt-config = { workspace = true }
tt-git = { workspace = true }
tt-store = { workspace = true }
# For the `TT_MCP_PORT` claim in a checkout's rendered `.env` — `port` reads it
# as a claim rather than parsing it, so app and client agree with the siblings.
tt-tasks = { workspace = true }
base64 = { workspace = true }
chrono = { workspace = true }
log = { workspace = true }
schemars = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
# Validates every tool result against its own `outputSchema` in the test helpers.
jsonschema = { version = "0.52.1", default-features = false }
tempfile = "3.14.0"

[lints]
workspace = true
