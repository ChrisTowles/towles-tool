---
description: Generate a git commit message from staged changes
---

Use the git_commit_generate tool to analyze staged changes and generate a conventional commit message. Review the diff and recent commits, then create an appropriate commit message following the format:

type(scope): description

Where type is one of: feat, fix, docs, style, refactor, test, chore
